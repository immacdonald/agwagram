import { designTokens, DynamicHeader } from 'phantom-library';
import style from './Header.module.scss';

const Header: React.FC = () => {
    return (
        <p>No</p>
    );
};

export { Header };
